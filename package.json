{
  "name": "collaborative-code-editor",
  "version": "1.0.0",
  "description": "Real-time collaborative code editor with video chat",
  "main": "api/index.js",
  "scripts": {
    "start": "cd api && npm install && node index.js",
    "install-all": "cd api && npm install && cd ../editor && npm install",
    "dev": "concurrently \"cd api && npm start\" \"cd editor && npm start\""
  },
  "engines": {
    "node": ">=14.x"
  }
}
